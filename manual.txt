GENERAL
==============================================================
token : https://github.com/settings/tokens/5280741259
repositorie : https://github.com/ceticon/testhttps.git
Project folder : /home/nsext/Documents/Code/Python/Projects/testhttps


spara token : 
nano ~/.git-credentials
https://ceticon:DIN_TOKEN@github.com
chmod 600 ~/.git-credentials

Du kan kontrollera att Git anvÃ¤nder credential helper:
git config --global credential.helper
bör svara : store

nano .gitignore :
=============================
myenv/
myvenv/
venv/

__pycache__/
*.pyc
.env
.vscode/
===========================


5.initiera Git : git init
6.Lägg till filerna : git add .
6.kontrollera : git status
7.Skapa första commit : git commit -m "Första versionen"
8.Koppla mot GitHub : git remote add origin https://github.com/ceticon/testhttps.git
	kontrollera : gitremote -v : bör svara
		origin	https://github.com/ceticon/testhttps.git (fetch)
		origin	https://github.com/ceticon/testhttps.git (push)
9. Byt till main : git branch -M main
10. Ladda upp : git push -u origin main


============================
1. git init
2. git add .
3. git commit -m "Första versionen"
4. git remote add origin https://github.com/ceticon/testhttps.git
5. git branch -M main
6. git push -u origin main  : git push -u origin main --force

=================================================
Nytt projekt
1. cd MittProjekt
2. 
3. git init
4. git add .
5. git commit -m "Första versionen"
6.
7. git remote add origin https://github.com/ceticon/MittProjekt.git
8. 
9. git branch -M main
10. git push -u origin main
=====================================================

Ny version av ett befintligt projekt
1. git add .
2. git commit -m "Fixade bugg i temperaturhantering"
3. git push
====================================================
PIP
pip freeze > requirements.txt

Lägg till den i gitHub
1. git add requirements.txt
2. git commit -m "Lade till requirements"
3. git push


På nästa raspberry Pi
pip install -r requirements.txt

====================================================
TOKEN
Spara GitHub-token lokalt för HTTPS
1. Aktivera lagring av credentials
	git config --global credential.helper store
Kontrollera: git config --global credential.helper
	borde svara : store
2. Skapa eller uppdatera filen .git-credentials
	nano ~/.git-credentials
	>>https://ceticon:GITHUB_TOKEN@github.com<<
	spara
3. Skydda filen
	chmod 600 ~/.git-credentials
4. Kontrollera att filen finns
	ls -l ~/.git-credentials
	borde svara
	-rw------- .... /home/nsext/.git-credential


Alternativ 2

1. git config --global credential.helper store

Sedan vid fö¶rsta: git push

anger jag
Username: ceticon
Password: <min token>

Git sparar dÃ¥ tokenen automatiskt i:
~/.git-credentials

Om du vill se att en credential finns sparad:
cat ~/.git-credentials

===============================================
SSH
1. Kontrollera om SSH-nyckel redan finns
ls -la ~/.ssh

Om du ser: 
id_rsa
id_rsa.pub

eller

id_ed25519
id_ed25519.pub

så har du redan en SSH-nyckel.

2. Skapa ny SSH-nyckel (om ingen finns)
	ssh-keygen -t ed25519 -C "min_epost@example.com"
	
	Tryck Enter på frågorna.
	
	Det skapar:
	~/.ssh/id_ed25519
	~/.ssh/id_ed25519.pub
	
3. Visa den publika nyckeln	
	cat ~/.ssh/id_ed25519.pub
	eller
	cat ~/.ssh/id_rsa.pub
	
4. Lägg till nyckeln på GitHub	
`I GitHub Settings
	SSH and GPG keys
    	New SSH key
	
5. Testa anslutningen
	ssh -T git@github.com
	
	Om allt fungerar får du något liknande:
	Hi ceticon! You've successfully authenticated,
	but GitHub does not provide shell access.
	
6. Använd SSH-repo	
	I stället för:
	https://github.com/ceticon/projekt.git
	anvä¤nder du:
	git@github.com:ceticon/projekt.git

	Exempel:
	git remote add origin git@github.com:ceticon/testhub.git
	
7. Pusha	
	git push
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	




